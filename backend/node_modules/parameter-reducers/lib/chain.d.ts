import { ParameterReducer } from './types';
export declare type Chain<T> = ParameterReducer<T> & {
    addParam: <S>(param: ParameterReducer<S>) => Chain<T & S>;
};
export declare function startChain<T>(param?: ParameterReducer<T>): Chain<T>;
