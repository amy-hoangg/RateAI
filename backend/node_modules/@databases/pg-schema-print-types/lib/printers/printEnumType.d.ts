import { EnumType } from '@databases/pg-schema-introspect';
import { FileContext } from '@databases/shared-print-types';
import PgPrintContext from '../PgPrintContext';
export declare enum EnumTypeMode {
    EnumType = 0,
    UnionAlias = 1,
    UnionAliasWithObject = 2,
    Inline = 3
}
export default function printEnumType(type: EnumType, context: PgPrintContext, file: FileContext): string;
