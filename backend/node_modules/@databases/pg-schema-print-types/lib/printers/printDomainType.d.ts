import { DomainType } from '@databases/pg-schema-introspect';
import { FileContext } from '@databases/shared-print-types';
import PgPrintContext from '../PgPrintContext';
export declare enum DomainTypeMode {
    StrictBrand = 0,
    LooseBrand = 1,
    Alias = 2,
    Inline = 3
}
export default function printDomainType(type: DomainType, context: PgPrintContext, file: FileContext): string;
