import { Queryable } from '@databases/pg';
interface Options {
    includeNonExistantSchemas?: boolean;
}
export default function getSearchPath(connection: Queryable, options?: Options): Promise<string[]>;
export {};
