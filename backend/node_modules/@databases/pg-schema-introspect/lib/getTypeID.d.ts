import { Queryable } from '@databases/pg';
export interface TypeIdQuery {
    schemaID?: number;
    schemaName?: string;
    typeName: string;
}
export default function getTypeID(connection: Queryable, query: TypeIdQuery): Promise<number>;
