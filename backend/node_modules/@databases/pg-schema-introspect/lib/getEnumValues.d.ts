import { Queryable } from '@databases/pg';
import { TypeQuery } from './getTypes';
export interface EnumValue {
    schemaID: number;
    schemaName: string;
    typeID: number;
    typeName: string;
    value: string;
}
export default function getEnumValues(connection: Queryable, query: TypeQuery): Promise<EnumValue[]>;
